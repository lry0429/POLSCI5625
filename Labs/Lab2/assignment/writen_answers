# Required written responses
# Ruiyuan Li


1. If `m = length(x)` and `n = length(y)`, how many pairwise distances must be
   calculated?
   
   - m*n pairwise distances must be calculated.
   
2. What is the time complexity of Tasks 3 and 4? State the result using Big-O
   or Big-Theta notation.
   
   - Task 3: O(mn) = O(3,000 * 5,000) = O(15,000,000)
   - Task 4: O(mn) = O(3,000 * 5,000) = O(15,000,000)
   
3. Does vectorization change the asymptotic complexity, or does it reduce
   implementation costs and constant factors?
   
   - Vectorization does not change the asymptotic time complexity, because both 
   the loop-based and vectorized approaches require O(mn) pairwise computations. 
   Vectorization reduces implementation overhead and constant factors by moving
   the iteration into optimized lower-level code.
   
4. What speedup and percentage time reduction did you observe from Task 3 to
   Task 4? Was the refactoring useful?
   
   - I did not observe speedup and percentage time reduction, because their
   elapsed_seconds are all close to 0 so that the difference is too small to
   observe, but the refactoring useful should be useful for larger dataset.
   
5. Let `k` be the number of nonzero entries after thresholding. Compare the
   storage complexity of the dense and sparse results.
   
   - Dense results: O(mn) = O(15,000,000)
   - Sparse results: O(nonzero_entries + n) = O(3,703,301 + 5,000) = O(3,708,301)
   
6. What percentage of final-object memory did Task 5 save relative to Task 4?
   
   - 100 - 62.94913 = 37.05087

7. Why can Task 5 produce a smaller final object while still using substantial
   peak memory during conversion from dense to sparse form?
   
   - The sparse result has a smaller final object because it does not explicitly 
   store zeros. However, converting from a dense matrix requires the full dense 
   matrix to exist first, and the sparse representation may be allocated before 
   the dense object is released. Therefore, peak memory during construction can 
   remain substantial even though the final sparse object is much smaller.
   